get:
  summary: List Balance Transactions
  description: List the balance transactions for your account. This API is limited to a single sub account or payout. This endpoint supports [pagination](https://docs.justifi.tech/api-spec#section/Pagination).
  operationId: ListBalanceTransactions
  tags:
    - Balance Transactions
  parameters:
    - $ref: ../components/parameters/authorization-header.yaml
    - $ref: ../components/parameters/sub-account.yaml
    - in: query
      name: payout_id
      schema:
        type: string
      required: false
      example: "po_123xyz"
      description: |
        Filter records which are part of the payout with the specified id
    - in: query
      name: source_payment_id
      schema:
        type: string
      required: false
      example: "py_123xyz"
      description: |
        Filter records which are associated with the payment with the specified id

  responses:
    '200':
      description: Successfully list balance transactions
      content:
        application/json:
          schema:
            allOf:
              - $ref: ../components/schemas/Envelope-list.yaml
              - properties:
                  type:
                    example: array
                  data:
                    items:
                      $ref: ../components/schemas/BalanceTransaction.yaml
