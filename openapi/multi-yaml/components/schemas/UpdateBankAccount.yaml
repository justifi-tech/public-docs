type: object
description: Bank Account
properties:
  metadata:
    type: object
    format: json
    description: any useful information you'd like to store alongside this bank account; when you update metadata, any previous metadata will be overwritten
    example: { new: info }
